name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - run: mv /dev/disk /dev/nondisk
      - run: pip install pyarmor
      - run: pyarmor -g 1
